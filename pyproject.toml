[project]
name = "tapeset"
version = "0.1.0"
description = "High-performance tar utility for Python."
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"
